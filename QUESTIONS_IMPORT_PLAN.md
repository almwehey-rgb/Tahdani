# خطة استيراد الأسئلة

أداة لتعبئة الفئات الفارغة بالأسئلة من ملفات markdown، بدون المساس بأي فئة
فيها أسئلة أصلًا.

## الوضع الحالي في قاعدة البيانات

مقروء مباشرة من الـ API الحيّ:

| | العدد |
|---|---|
| مجموع الفئات | 594 |
| فئات فيها أسئلة | 35 |
| فئات فارغة | 559 |
| فئات فارغة لكن عندها صورة (الأهداف الحقيقية للاستيراد) | 134 |
| مجموع الأسئلة الموجودة | ~2,470 |

الـ 559 فئة الفارغة أغلبها placeholders أُنشئت دفعة واحدة. الـ 134 اللي
عندها صور هي اللي تستاهل تعبئة — الباقي أسماء بلا محتوى ولا صورة.

## الملفات

```
QUESTIONS_IMPORT_PLAN.md                     ← هذا الملف
server/prisma/importMarkdownQuestions.ts     ← السكربت
server/prisma/import-data/TEMPLATE.md        ← نموذج التنسيق
server/prisma/import-data/categoryMap.json   ← ربط اسم ملف باسم فئة
server/prisma/import-data/questions/*.md     ← ملفات الأسئلة (ضعها هنا)
```

## التشغيل

من مجلد `server/`:

```bash
# 1) فحص الملفات فقط — لا يتصل بقاعدة البيانات إطلاقًا
npm run import:questions -- --offline
npm run import:questions -- --offline --verbose   # يطبع كل سؤال وجوابه

# 2) تشغيل تجريبي مع مطابقة الفئات — يقرأ ولا يكتب
ADMIN_CODE=... npm run import:questions -- --api

# 3) الكتابة الفعلية
ADMIN_CODE=... npm run import:questions -- --api --commit

# ملف واحد فقط
ADMIN_CODE=... npm run import:questions -- --api --only "عالم العطور"
```

### طريقتان للوصول لقاعدة البيانات

| | يحتاج | متى تستخدمه |
|---|---|---|
| **`--api`** | `ADMIN_CODE` فقط | الإنتاج. ما يحتاج كلمة سر قاعدة البيانات. أبطأ (طلب لكل سؤال). |
| الافتراضي (Prisma) | `DATABASE_URL` في `server/.env` | محليًا، أو لما تكون كلمة سر القاعدة متوفرة. أسرع بكثير. |

متغيرات `--api` الاختيارية: `API_BASE` (افتراضيًا
`https://tahdani-server.vercel.app`)، `ADMIN_NAME` (افتراضيًا `مشرف تحدّني`).

## الضمانات — ما الذي يمنع ضياع البيانات

السكربت **يضيف فقط**، وفقط في فئة فارغة تمامًا:

1. **لا ينشئ فئات.** ملف اسمه ما يطابق أي فئة موجودة → يُتخطّى ويُذكر في التقرير.
2. **لا يكتب فوق موجود.** فئة فيها ولو سؤال واحد → تُتخطّى.
3. **مطابقة حرفية للاسم.** لا تخمين ولا تقريب. اسم مكرر في فئتين → يُتخطّى حتى
   توضّحه في `categoryMap.json`.
4. **قائمة فئات محميّة** مكتوبة داخل السكربت، تُرفض حتى لو صارت فارغة:
   تاريخ، تكنولوجيا، سيارات، دول وعواصم، عالم الحيوان، قصص الأنبياء،
   كأس العالم 2026، مسيرة لاعب، مطاعم الكويت، معلومات عامة.
5. **إعادة فحص قبل الكتابة مباشرة.** لو أحد أضاف سؤالًا من لوحة التحكم بين
   الفحص والكتابة، تُترك الفئة كما هي.
6. **التشغيل التجريبي هو الافتراضي.** بدون `--commit` ما تُكتب ولا بايت.

الفحوصات مجرّبة فعليًا: ملفات اختبار لفئة محميّة، ولفئة فيها أسئلة، ولاسم
مكرر، ولاسم غير موجود، ولملف بتنسيق خاطئ — كلها تُتخطّى بالسبب الصحيح.

## تنسيق الملف

اسم الفئة يُؤخذ من العنوان الأول `# ...` (أو من اسم الملف لو ما فيه عنوان).

```markdown
# عالم العطور

## 100

س: ما هي المدينة الفرنسية التي تُلقّب بعاصمة العطور؟
ج: غراس
تلميح: تقع في جنوب فرنسا قرب كان

## 200

س: أي زهرة تُقطف فجرًا لأن رائحتها تتبخر مع الشروق؟
ج: الياسمين
```

`## 100` يضبط النقاط لكل ما تحته. البلوكات تُفصل بسطر فارغ.

الحقول: `س` (إجباري)، `ج` (إجباري)، `تلميح`، `صورة`، `فيديو`،
`أبيض وأسود`، `نقاط`. النقاط المسموحة: 100–600.

يُقبل أيضًا جدول — مناسب للتصدير من Excel:

```markdown
| السؤال | الإجابة | النقاط | تلميح |
|---|---|---|---|
| في أي قارة تقع مصر؟ | أفريقيا | 100 | قارة سمراء |
```

## ⚠️ مشكلة قائمة تمنع الاستيراد: 12 اسم فئة مكرر

فيه 12 اسم موجود مرتين في قاعدة البيانات. النسخة الثانية في كلها من نفس
الدفعة (`cmt07cb4…`) وأغلبها فارغة وبدون صورة:

| الاسم | النسخة الأصلية | النسخة المكررة |
|---|---|---|
| رياضة | 77 سؤال + صورة | 0 سؤال، بدون صورة |
| جغرافيا | 77 + صورة | 0 |
| تكنولوجيا | 77 + صورة | 0 |
| معلومات عامة | 77 + صورة | 0 |
| عالم الحيوان | 60 + صورة | 0 |
| مطاعم الكويت | 60 + صورة | 0 |
| قصص الأنبياء | 60 + صورة | 0 |
| كأس العالم 2026 | 60 + صورة | 0 |
| سيارات | 50 + صورة | 0 |
| من سجل الهدف | 20 سؤال | 0 |
| **تاريخ** | 77 + صورة | **77 سؤال** (محتوى مكرر فعليًا) |
| **مسيرة لاعب** | 60 + صورة | **60 سؤال** (محتوى مكرر فعليًا) |

عشر منها آمنة للتعطيل (فارغة أصلًا). اثنتان — تاريخ ومسيرة لاعب — فيهما
137 سؤال مكرر يظهر للاعبين مرتين، ويحتاج قرار قبل حذفه.

**لم يُحذف ولم يُعطَّل أي شيء** — هذا تقرير فقط.
