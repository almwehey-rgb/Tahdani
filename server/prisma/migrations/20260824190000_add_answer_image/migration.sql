-- A picture of the word to act out, shown on the judge's sheet so the actor
-- works from an image instead of reading a label out to themselves.
ALTER TABLE "QuestionAnswer" ADD COLUMN "imageUrl" TEXT;
