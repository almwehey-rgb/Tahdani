import { app } from '../src/app';

// Vercel's Node.js runtime accepts any (req, res) handler, and an Express
// app is exactly that — so it can be exported directly as the function.
export default app;
